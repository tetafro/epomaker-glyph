version: 2
builds:
  - goos: [linux]
    goarch: [amd64]
    env: [CGO_ENABLED=1]
checksum:
  name_template: checksums.txt
snapshot:
  version_template: "{{ .Tag }}"
changelog:
  sort: asc
  filters:
    exclude:
      - '^Merge pull request'
