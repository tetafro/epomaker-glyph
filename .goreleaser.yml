version: 2
builds:
  goos: [linux]
  goarch: [amd64, arm64]
checksum:
  name_template: checksums.txt
snapshot:
  version_template: "{{ .Tag }}"
changelog:
  sort: asc
  filters:
    exclude:
      - '^Merge pull request'
